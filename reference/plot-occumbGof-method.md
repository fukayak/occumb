# Plot method for occumbGof class.

Draws a scatter plot of fit statistics.

## Usage

``` r
# S4 method for class 'occumbGof'
plot(x, y = NULL, ...)
```

## Arguments

- x:

  An `occumbGof` object.

- y:

  `NULL`

- ...:

  Additional arguments passed to the default `plot` method.

## Value

Returns `NULL` invisibly.
