# Summary method for occumbData class.

Summarizes dataset stored in an `occumbData` object.

## Usage

``` r
# S4 method for class 'occumbData'
summary(object)
```

## Arguments

- object:

  An `occumbData` object.

## Value

Returns `NULL` invisibly.
